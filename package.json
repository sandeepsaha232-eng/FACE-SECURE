{
    "name": "facesecure-root",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
        "install": "cd frontend && npm install && cd ../backend && npm install"
    }
}